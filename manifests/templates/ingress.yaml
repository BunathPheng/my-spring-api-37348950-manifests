---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-spring-api-37348950                  # Ingress name
  labels:
    app: my-spring-api-37348950
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "my-spring-api-37348950.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-spring-api-37348950               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
